class Restaurant < ActiveRecord::Base
  searchkick autocomplete: ['name']
  
  validates :name, :uniqueness => {:scope => [:address1, :zip], message: 'This restaurant has already been created.'}

end
